/* 1. Main Screen Logic */
.screen-root {
    flex-grow: 1;
    justify-content: center;
    padding: 1.5%;
    -unity-background-image-tint-color: darkgrey;
    -unity-background-scale-mode: scale-and-crop;
}

/* 2. Layout Containers */
.cont-carousel-viewport {
    flex-direction: row;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 80%;
}

/* 3. Elements */
.btn-level {
    height: 100%;
    aspect-ratio: 1;
    border-radius: 50%;
    background-color: white;
    justify-content: center;
    align-items: center;
}

.btn-level-image {
    height: 90%;
    aspect-ratio: 1;
    border-radius: 50%;
}

.lbl-level-title {
    align-self: center;
    height: auto;
    font-size: 65px;
    -unity-font-style: bold;
    -unity-font-definition: url("project://database/Assets/TextMesh Pro/Fonts/LiberationSans.ttf");
}

/* 4. Utilities */
.u-hide {
    display: none;
}

.u-flex-spacer {
    flex-grow: 1;
}